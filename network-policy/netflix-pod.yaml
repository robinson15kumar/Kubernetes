apiVersion : v1
kind : Pod
metadata :
  name : netflix-pod
  labels: 
    type : laila
    height: 4foot
spec :
  containers :
    - name : netflixcontainer
      image : devopsinsiders/netflix-clone
      ports :
        - containerPort : 80