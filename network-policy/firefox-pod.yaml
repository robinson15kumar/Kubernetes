apiVersion : v1
kind : Pod
metadata :
  name : firefox-pod
  labels :
    type : majnu
    height: 5foot
spec :
  containers :
    - name : firefoxcontainer
      image : linuxserver/firefox
      ports :
        - containerPort : 3000