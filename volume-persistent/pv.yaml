apiVersion : v1
kind : PersistentVolume
metadata : 
  name : myvolumepv
spec:
  accessModes :
  - ReadWriteMany
  azureFile :
    secretName : merephotosksecret
    shareName : myphotoshare
  capacity :
    storage : 4Gi
  storageClassName : azurefile
  volumeMode :  Filesystem