apiVersion : v1
kind : Pod
metadata :
  name : firefox-pod
  labels :
    dev: env
spec :
  containers :
    - name : firefoxcontainer
      image : linuxserver/firefox
      ports :
        - containerPort : 3000