# ClusterIP Service
apiVersion: v1
kind: Service
metadata: 
  name: rob-service-cluster
spec:
  selector:
    env: dev
  type: ClusterIP
  ports:
  - port: 9000  
    targetPort: 80

# selector contains labels of my pods 
# port is my service port and targetPort is port of my pods 